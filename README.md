# ZKSync advanced transaction explorer

It fetches information from multiple public RPCs and combines it together to show more detailed information about the executed transactions.

To run locally:

```bash
npm install
npm run dev
```